{
  "name": "nginx-docs-theme-test",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "test": "playwright test --workers 6"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1",
    "dotenv": "17.2.3"
  }
}
